stages:
  - build

variables:
  NODE_ENV: production

before_script:
  - npm install

build:
  stage: build
  script:
    - npm run lint
    - npm test
    - npm run build
  artifacts:
    paths:
      - .next